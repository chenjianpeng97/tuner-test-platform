{
    "name": "tuner-test-platform",
    "private": true,
    "packageManager": "pnpm@10.18.3",
    "scripts": {
        "nx": "nx",
        "graph": "nx graph",
        "show:projects": "nx show projects",
        "init": "pnpm install && nx run-many --target=init --all"
    },
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "nx": "^21.0.0"
    }
}